#!/usr/bin/env sh
# desc: Serve discern.sh against the sibling design-system checkout; pass a path to override it (deno task site:design-system)
set -eu
cd "${DISCERN_ROOT:-$(dirname "$0")/../..}"
exec deno task site:design-system "$@"
