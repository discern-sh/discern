---
title: Reference
description: Generated and contract reference material for the discern surface.
publish: false
---

# Reference

_Reference material for the discern surface — generated where a registry can produce it, authored where a contract needs prose._

This section is a stub: the reference tier is planned but not yet authored or published, so every page here carries `publish: false` and stays off every published surface until the authoring pass lands.

What already lives here:

- [cli-reference.md](cli-reference.md) — every `discern` command, subcommand, and flag, **generated** by `deno task codegen` from the live command registry (never hand-edited; a drift test fails the gate when it goes stale).

Planned for the authoring pass: the `discern.toml` reference (today under [Getting started](../10-getting-started/config-reference.md)), the MCP tools and result envelope contract, file ownership, identity tokens, and supported platforms.
