# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# A call to action whose whole label is a generic verb tells the reader
# nothing about the destination. Matches the raw link syntax (raw scope:
# rendered text loses the markup), so only a label that IS the whole CTA
# fires. Warning severity: a secondary link may earn a short label.
# Sources: proposed check brand/cta-generic-label.
extends: existence
message: "Generic CTA label ('%s'): name the destination or commitment instead."
ignorecase: true
level: warning
scope: raw
raw:
  - \[(?:Learn\s+more|Explore|Discover)\]\(
