# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# Model-copy opening templates. The house style already blocks 'in a world
# where' map-wide at error severity; this rule adds the registers' other named
# templates. Warning severity: the brand pages legitimately quote these
# templates when teaching against them.
# Sources: brand voice rule banned-moves/this-isnt-just-openings; brand voice
# rule banned-moves/the-future-of-claims.
extends: existence
message: "Model-copy template ('%s'): make the claim directly."
ignorecase: true
level: warning
tokens:
  - this\s+is(?:n(?:'|’)t|\s+not)\s+just
  - the\s+future\s+of
