# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# The product name is written lower-case in every position, including at the
# start of a sentence — recast the line rather than capitalizing it. Zero
# legitimate exceptions in the canon, so it blocks the gate; compound code
# identifiers (style and type names) keep their own word boundaries, and code
# spans are never linted.
# Sources: agent voice rule language-rules/write-the-product-name-as-discern.
extends: existence
message: "The product name is always lower-case 'discern': recast the sentence rather than capitalizing the name."
level: error
tokens:
  - Discern
