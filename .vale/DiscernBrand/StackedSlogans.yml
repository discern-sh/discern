# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# Three consecutive slogan-length sentences read as stacked aphorisms.
# Suggestion severity: spec fragments listing facts are legal, so this prompts
# a look rather than asserting a defect.
# Sources: brand voice principle
# write-for-the-page-not-for-a-slogan-collection.
extends: existence
message: "Three consecutive slogan-length sentences: let one line land and give it support."
level: suggestion
nonword: true
scope: paragraph
tokens:
  - (?:\b[\w’-]+(?:\s+[\w’-]+){0,3}[.!?]\s+){2}\b[\w’-]+(?:\s+[\w’-]+){0,3}[.!?]
