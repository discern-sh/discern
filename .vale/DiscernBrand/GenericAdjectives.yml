# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# Adjectives that dodge 'compared to what?'. The house style already blocks
# 'seamless', 'robust', and 'powerful' map-wide at error severity; this rule
# adds the proposal's remaining forms. Warning severity: the fix is showing
# the mechanism, which is the writer's call.
# Sources: proposed check brand/unsupported-adjectives; brand voice principle
# make-confidence-feel-earned.
extends: existence
message: "'%s' dodges 'compared to what?': show the mechanism or artifact that earned it."
ignorecase: true
level: warning
tokens:
  - enterprise[-\s]grade
  - transformative(?:ly)?
