# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# Generic transformation verbs with no concrete content. The house style
# already blocks 'unlock' and 'empower' map-wide at error severity; this rule
# adds the proposal's remaining pair. Warning severity: 'transform' can name a
# real operation, so the writer judges.
# Sources: proposed check brand/generic-verbs; brand voice rule
# banned-moves/unlock-empower-reimagine-and-seamless.
extends: existence
message: "'%s' is a generic verb: name the concrete change instead."
ignorecase: true
level: warning
tokens:
  - transform(?:s|ed|ing)?
  - reimagin(?:e[sd]?|ing)
