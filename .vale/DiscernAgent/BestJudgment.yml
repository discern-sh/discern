# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# Deferring to an agent's judgment where a real boundary can be stated leaves
# the boundary unstated. Warning severity by the rule's own qualifier: where
# no boundary can be stated, the phrase is legitimate.
# Sources: agent voice rule language-rules/do-not-say-use-your-best-judgment.
extends: existence
message: "'%s': state the real boundary instead, where one can be stated."
ignorecase: true
level: warning
tokens:
  - use\s+your\s+(?:best\s+|own\s+)?judg(?:e)?ment
