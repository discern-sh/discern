# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# A positional reference breaks the moment context is compressed or reordered;
# a stable target (a path, heading, or anchor) survives. Warning severity:
# prose about a directly preceding block can occasionally earn it.
# Sources: agent voice rule
# language-rules/prefer-stable-targets-over-positional-references.
extends: existence
message: "Positional reference ('%s'): point at a stable target (a path, heading, or anchor) instead."
ignorecase: true
level: warning
tokens:
  - (?:as\s+)?(?:mentioned|noted|shown|described|listed)\s+above
  - see\s+above
  - the\s+(?:file|command|section|table|list|example|snippet|step|output)\s+above
