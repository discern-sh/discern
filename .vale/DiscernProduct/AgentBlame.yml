# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# Product surfaces describe the object's state, never a worker's character:
# name the branch, tree, or command, not what the agent supposedly is. Warning
# severity: an analytical page may describe recorded behavior, so the writer
# judges the verdict.
# Sources: proposed check product/agent-blame; product voice principle
# name-the-object-in-the-state; product voice rule
# anti-patterns/blames-or-praises-an-agents-character.
extends: existence
message: "Agent-blame vocabulary ('%s'): describe the object's state, not the worker's character."
ignorecase: true
level: warning
tokens:
  - the\s+agent\s+(?:forgot|neglected|didn(?:'|’)t\s+bother)
  - (?:careless|lazy|stupid|dumb|incompetent|untrustworthy|unreliable)\s+agents?
  - agents?\s+(?:is|are|was|were)\s+(?:careless|lazy|stupid|dumb|incompetent|untrustworthy|unreliable)
  - blames?\s+the\s+agent
