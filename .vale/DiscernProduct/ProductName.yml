# GENERATED by `deno task codegen` from VALE_STYLE_RULES (scripts/brand/vale.ts) — do NOT edit by hand. Change the brand registry (scripts/brand/) and regenerate.
#
# The product name is lower-case on human-facing product surfaces. The prose
# gate holds this warning at zero on every maintained page where the product
# register applies. Brand pages enforce their sibling rule; ADRs retain
# reduced styling. Compound code identifiers keep their own word boundaries,
# and code spans are never linted.
# Sources: proposed check product/canonical-glossary-terms; agent voice rule
# language-rules/write-the-product-name-as-discern.
extends: existence
message: "The product name is lower-case 'discern': recast the sentence rather than capitalizing the name."
level: warning
tokens:
  - Discern
