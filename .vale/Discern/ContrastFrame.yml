# The "not X, but Y" family — the strongest machine-written fingerprint (voice
# skill: banned moves). Docs budget: one per page, and only when the
# distinction does real work; marketing budget: zero.
extends: existence
message: "Contrast-frame ('not X, but Y'): state the true half plainly, unless the distinction does real work."
ignorecase: true
level: warning
tokens:
  - not\s+[^.?!;:—]{1,45},\s+but
  - isn(?:'|’)t\s+[^.?!;:—]{1,45}[,;]\s+it(?:'|’)s
  - wasn(?:'|’)t\s+[^.?!;:—]{1,45}[,;]\s+it\s+(?:was|is)
  - was\s+never\s+the\s+point
  - not\s+about\s+[^.?!;:—]{1,45}[,;.]\s+it(?:'|’)s\s+about
