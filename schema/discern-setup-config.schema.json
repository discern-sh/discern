{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://discern.sh/schema/v1/discern-setup-config.schema.json",
  "x-discern-compatibility-policy": "config-input",
  "title": "discern setup config document",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "A pointer to this schema for your editor. discern ignores it.",
      "type": "string"
    },
    "version": {
      "description": "The document's major version. Leave it out to use the current one, or give a matching major version; this discern understands version 1.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "number"
        }
      ]
    },
    "name": {
      "description": "The project's display name, in any words.",
      "type": "string"
    },
    "slug": {
      "description": "A short id for the project: lowercase letters, digits, and dashes, starting with a letter or digit.",
      "type": "string"
    },
    "branch_prefix": {
      "description": "The start of every worktree branch name, such as \"agent/\".",
      "type": "string"
    },
    "brief": {
      "description": "What the project is, in your own words. discern saves it as the project brief.",
      "type": "string"
    },
    "agents": {
      "description": "Which coding agents to set up, with the instruction file each reads: claude_code -> CLAUDE.md, gemini -> GEMINI.md, codex / cursor / copilot -> AGENTS.md.",
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "claude_code",
          "codex",
          "gemini",
          "cursor",
          "copilot"
        ]
      }
    },
    "map": {
      "description": "`[map]` settings: the folder for the project map.",
      "default": {},
      "type": "object",
      "properties": {
        "dir": {
          "default": "discern/map",
          "description": "The map's folder, relative to the project root. `discern setup begin` creates it here if it doesn't exist, and `discern map` reads it. Changing it doesn't move any files.",
          "type": "string",
          "pattern": "^(?!.*\\s\\/?$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*\\/?$"
        }
      },
      "additionalProperties": false
    },
    "jobs": {
      "description": "`[jobs]` entries. A known name takes a command, a list, or a table such as { run, timeout }, and has a fixed stage; a custom `[jobs.<name>]` table needs `stage` and `run`.",
      "allOf": [
        {
          "type": "object",
          "propertyNames": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]+$"
          },
          "additionalProperties": {}
        },
        {
          "type": "object",
          "properties": {
            "format": {
              "description": "A formatter, or another tool that rewrites files. Because it changes files, it runs first, one command at a time. In `discern prepare` its edits stay for you to commit; in `discern done`, a change to a committed file fails the run.",
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "object",
                  "properties": {
                    "run": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        }
                      ],
                      "description": "The command to run, or a list of commands run in order, each only if the previous one succeeded. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
                    },
                    "timeout": {
                      "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
                      "type": "number",
                      "minimum": 0
                    },
                    "inputs": {
                      "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
                      "minItems": 1,
                      "type": "array",
                      "items": {
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "needs": {
                      "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
                      }
                    },
                    "artifacts": {
                      "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    },
                    "environment": {
                      "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
                      }
                    },
                    "toolchain": {
                      "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    }
                  },
                  "required": [
                    "run"
                  ],
                  "additionalProperties": false
                }
              ]
            },
            "build": {
              "description": "Builds what later stages need, such as compiling or bundling. `discern done` runs it; `discern prepare` doesn't.",
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "object",
                  "properties": {
                    "run": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        }
                      ],
                      "description": "The command to run, or a list of commands run in order, each only if the previous one succeeded. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
                    },
                    "timeout": {
                      "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
                      "type": "number",
                      "minimum": 0
                    },
                    "inputs": {
                      "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
                      "minItems": 1,
                      "type": "array",
                      "items": {
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "needs": {
                      "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
                      }
                    },
                    "artifacts": {
                      "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    },
                    "environment": {
                      "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
                      }
                    },
                    "toolchain": {
                      "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    }
                  },
                  "required": [
                    "run"
                  ],
                  "additionalProperties": false
                }
              ]
            },
            "lint": {
              "description": "A linter or other read-only check of the code.",
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "object",
                  "properties": {
                    "run": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        }
                      ],
                      "description": "The command to run, or a list of commands run in order, each only if the previous one succeeded. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
                    },
                    "timeout": {
                      "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
                      "type": "number",
                      "minimum": 0
                    },
                    "inputs": {
                      "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
                      "minItems": 1,
                      "type": "array",
                      "items": {
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "needs": {
                      "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
                      }
                    },
                    "artifacts": {
                      "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    },
                    "environment": {
                      "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
                      }
                    },
                    "toolchain": {
                      "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    }
                  },
                  "required": [
                    "run"
                  ],
                  "additionalProperties": false
                }
              ]
            },
            "typecheck": {
              "description": "A read-only type check.",
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "object",
                  "properties": {
                    "run": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        }
                      ],
                      "description": "The command to run, or a list of commands run in order, each only if the previous one succeeded. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
                    },
                    "timeout": {
                      "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
                      "type": "number",
                      "minimum": 0
                    },
                    "inputs": {
                      "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
                      "minItems": 1,
                      "type": "array",
                      "items": {
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "needs": {
                      "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
                      }
                    },
                    "artifacts": {
                      "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    },
                    "environment": {
                      "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
                      }
                    },
                    "toolchain": {
                      "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    }
                  },
                  "required": [
                    "run"
                  ],
                  "additionalProperties": false
                }
              ]
            },
            "test": {
              "description": "Your test suite. It waits for a free test-run slot before it starts.",
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "object",
                  "properties": {
                    "run": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        }
                      ],
                      "description": "The command to run, or a list of commands run in order, each only if the previous one succeeded. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
                    },
                    "timeout": {
                      "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
                      "type": "number",
                      "minimum": 0
                    },
                    "inputs": {
                      "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
                      "minItems": 1,
                      "type": "array",
                      "items": {
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "needs": {
                      "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
                      }
                    },
                    "artifacts": {
                      "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    },
                    "environment": {
                      "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
                      }
                    },
                    "toolchain": {
                      "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    }
                  },
                  "required": [
                    "run"
                  ],
                  "additionalProperties": false
                }
              ]
            },
            "smoke": {
              "description": "A quick check, with few side effects, that the app starts with real config in this checkout. `discern done` and `discern test` run it alongside the tests, so with `[gate].fail_fast` a quick failure stops the slower tests. It also runs in the main checkout after each landing.",
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "object",
                  "properties": {
                    "run": {
                      "anyOf": [
                        {
                          "type": "string"
                        },
                        {
                          "type": "array",
                          "items": {
                            "type": "string"
                          }
                        }
                      ],
                      "description": "The command to run, or a list of commands run in order, each only if the previous one succeeded. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
                    },
                    "timeout": {
                      "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
                      "type": "number",
                      "minimum": 0
                    },
                    "inputs": {
                      "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
                      "minItems": 1,
                      "type": "array",
                      "items": {
                        "type": "string",
                        "minLength": 1
                      }
                    },
                    "needs": {
                      "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
                      }
                    },
                    "artifacts": {
                      "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    },
                    "environment": {
                      "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
                      }
                    },
                    "toolchain": {
                      "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
                      "type": "array",
                      "items": {
                        "type": "string",
                        "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                      }
                    }
                  },
                  "required": [
                    "run"
                  ],
                  "additionalProperties": false
                }
              ]
            }
          },
          "additionalProperties": {
            "type": "object",
            "properties": {
              "stage": {
                "type": "string",
                "enum": [
                  "fix",
                  "build",
                  "check",
                  "test"
                ],
                "description": "The gate stage this job runs in: fix, build, check, or test."
              },
              "run": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                ],
                "description": "The command to run, or a list of commands run in order, each only if the previous one succeeded. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
              },
              "inputs": {
                "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
                "minItems": 1,
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "needs": {
                "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
                "type": "array",
                "items": {
                  "type": "string",
                  "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
                }
              },
              "artifacts": {
                "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
                "type": "array",
                "items": {
                  "type": "string",
                  "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                }
              },
              "environment": {
                "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
                "type": "array",
                "items": {
                  "type": "string",
                  "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
                }
              },
              "toolchain": {
                "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
                "type": "array",
                "items": {
                  "type": "string",
                  "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
                }
              },
              "provides": {
                "description": "A free-text note on what the job provides, for people reading the config. discern doesn't use it.",
                "type": "string"
              },
              "timeout": {
                "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
                "type": "number",
                "minimum": 0
              }
            },
            "required": [
              "stage",
              "run"
            ],
            "additionalProperties": false,
            "description": "A custom job: any name of letters, digits, `_`, or `-`, with its stage and command set out."
          }
        }
      ]
    },
    "scopes": {
      "description": "`[scopes.<name>]` tables: named regions defined by `paths`, with optional settings.",
      "type": "object",
      "propertyNames": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_-]+$"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The path patterns that define the scope: a folder prefix (`src/**`), a standard glob (`src/**/*.ext`, `src/*`), a `*.ext` suffix at any depth, a `/seg/` segment, or an exact path. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
          },
          "neutral": {
            "default": false,
            "description": "Set to true when changes here aren't code, as for documentation and agent instructions. They trigger no scope gate, this scope's own `gate` never runs, and coupling ignores them. The gate's jobs still run.",
            "type": "boolean"
          },
          "preview": {
            "description": "A read-only command your agent can run in its worktree to preview a change in this scope, such as building the docs. discern suggests it but never runs it. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written.",
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            ]
          },
          "inputs": {
            "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "needs": {
            "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
            }
          },
          "artifacts": {
            "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
            }
          },
          "environment": {
            "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
            }
          },
          "toolchain": {
            "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
            }
          },
          "gate": {
            "description": "A command `discern done` runs when a change touches this scope, such as a component's own checks. The reuse settings in this table apply to it. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written.",
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            ]
          },
          "timeout": {
            "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
            "type": "number",
            "minimum": 0
          }
        },
        "required": [
          "paths"
        ],
        "additionalProperties": false
      }
    },
    "generated": {
      "description": "`[generated.<name>]` tables: committed generated files and the command that regenerates them.",
      "type": "object",
      "propertyNames": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_-]+$"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "paths": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Path patterns for the committed files this generator owns entirely, in the scope glob syntax: a folder prefix (`reference/**`), a standard glob (`reference/**/*.md`, `reference/*`), a `*.ext` suffix at any depth, a `/seg/` segment, or an exact path. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
          },
          "run": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            ],
            "description": "The command, or list of commands, that rewrites these files. The same source must always produce the same bytes, and the command must delete any file it no longer generates. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written."
          },
          "linguist_generated": {
            "default": false,
            "description": "Set to true to mark these files as generated for GitHub, through the `linguist-generated` attribute. GitHub then collapses them in diffs and leaves them out of language statistics.",
            "type": "boolean"
          },
          "timeout": {
            "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
            "type": "number",
            "minimum": 0
          }
        },
        "required": [
          "paths",
          "run"
        ],
        "additionalProperties": false
      }
    },
    "standards": {
      "description": "`[standards.<name>]` tables. Any name works; coverage is only a common one.",
      "type": "object",
      "propertyNames": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_-]+$"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "metric": {
            "description": "The metric name discern reads from the output: the producer's, or the `extract` command's when `extract` is set. Default: the standard's name.",
            "type": "string"
          },
          "direction": {
            "type": "string",
            "enum": [
              "up",
              "down"
            ],
            "description": "\"up\" when higher is better, making the limit a floor; \"down\" when lower is better, making it a ceiling."
          },
          "limit": {
            "type": "number",
            "minimum": -9007199254740991,
            "description": "The floor or ceiling the measurement must meet; a measurement equal to the limit passes. A branch can raise a floor or lower a ceiling, but loosening one needs `discern standards propose` and the owner's approval."
          },
          "run": {
            "description": "The command that measures this standard. It prints `DISCERN_METRIC <metric> <number>`, unless `extract` reads its output instead. Set exactly one of `run` or `producer`.",
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "minItems": 1,
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              }
            ]
          },
          "producer": {
            "description": "Take the measurement from an existing job, scope gate, or standard, named like `jobs.test`, `scopes.<name>.gate`, or `standards.<name>`, instead of running a command of your own. That producer runs once for every standard that reads it. With `producer`, the standard's `needs`, `artifacts`, `environment`, and `toolchain` are ignored.",
            "type": "string",
            "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
          },
          "extract": {
            "description": "A second command that reads the producer's output, or the `artifact` file, on stdin and prints the `DISCERN_METRIC` line. With `extract`, discern reuses a measurement only when the standard declares `inputs`.",
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "minItems": 1,
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              }
            ]
          },
          "artifact": {
            "description": "A file the producer lists in its `artifacts`, passed to `extract` on stdin in place of the output. Needs `extract`.",
            "type": "string",
            "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
          },
          "inputs": {
            "description": "Every file pattern the command reads, in the scope glob syntax. With a complete list, discern can reuse an earlier result while those files and the command are unchanged; a file you leave out can let a stale result through. Without `inputs`, a result counts only for the commit it ran on.",
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "needs": {
            "description": "Other jobs, scope gates, or standards that must succeed before this command runs, named like `jobs.build`, `scopes.<name>.gate`, or `standards.<name>`.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^(?:jobs\\.[A-Za-z0-9_-]+|scopes\\.[A-Za-z0-9_-]+\\.gate|standards\\.[A-Za-z0-9_-]+)$"
            }
          },
          "artifacts": {
            "description": "Files the command produces, as exact paths relative to the project root. After each run, discern keeps its own copy, for example for a standard's `extract` to read.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
            }
          },
          "environment": {
            "description": "Environment variables whose values affect the result. discern records a hash of each value, and a changed value stops it reusing an earlier result. Listing a variable doesn't set it.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
            }
          },
          "toolchain": {
            "description": "Files that pin your tool versions, such as a lockfile, relative to the project root. A change to one stops discern reusing an earlier result.",
            "type": "array",
            "items": {
              "type": "string",
              "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
            }
          },
          "per": {
            "description": "Hold a rate instead of a raw count, so the number doesn't rise only because the project grew. Divide by a second metric the command prints, which must be above 0, or by a count discern takes itself: files, lines, words, or bytes in the files a pattern matches, for example per = { words = \"${map.dir}**\" }. Patterns use the scope glob syntax and match the files Git tracks or would track. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written.",
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "properties": {
                  "files": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "minItems": 1,
                        "type": "array",
                        "items": {
                          "type": "string"
                        }
                      }
                    ]
                  },
                  "lines": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "minItems": 1,
                        "type": "array",
                        "items": {
                          "type": "string"
                        }
                      }
                    ]
                  },
                  "words": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "minItems": 1,
                        "type": "array",
                        "items": {
                          "type": "string"
                        }
                      }
                    ]
                  },
                  "bytes": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "minItems": 1,
                        "type": "array",
                        "items": {
                          "type": "string"
                        }
                      }
                    ]
                  }
                },
                "additionalProperties": false
              }
            ]
          },
          "scale": {
            "default": 1,
            "description": "Multiply a `per` rate into readable units: scale = 1000 gives a rate per 1,000. It has no effect without `per`.",
            "type": "number",
            "exclusiveMinimum": 0
          },
          "margin": {
            "default": 0,
            "description": "Headroom `discern standards --pin` keeps when it tightens the limit to the measured value. Give a margin to a number that moves with unrelated changes, such as a size or a coverage percentage, so ordinary movement doesn't fail a pinned limit.",
            "type": "number",
            "minimum": 0
          },
          "timeout": {
            "description": "Time limit in seconds for this job, replacing `[gate].timeout`; 0 means no limit. Leave it out to use `[gate].timeout`.",
            "type": "number",
            "minimum": 0
          }
        },
        "required": [
          "direction",
          "limit"
        ],
        "additionalProperties": false
      }
    },
    "checkpoints": {
      "description": "`[checkpoints.<name>]` tables: review questions, with their triggers and mode.",
      "type": "object",
      "propertyNames": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_-]+$"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "scope": {
            "description": "The name of a configured scope whose paths this checkpoint watches. Set at most one of `scope` and `paths`; with neither, it watches every file people or agents changed.",
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]+$"
          },
          "paths": {
            "description": "Path patterns this checkpoint watches, in the scope glob syntax. Set at most one of `scope` and `paths`. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "include_generated": {
            "description": "Set to true to also watch files a `[generated.<name>]` group owns. By default, a checkpoint watches only files people and agents write.",
            "type": "boolean"
          },
          "exclude_paths": {
            "description": "Path patterns to ignore. discern removes them before any other trigger setting or `when` command looks at the change. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "unless_changed": {
            "description": "Stay quiet when the change also touches one of these path patterns or scopes, anywhere in the project, such as the docs that go with an interface. You can write `${map.dir}`, `${skills.dir}`, `${scripts.dir}`, `${project.todo}`, or `${project.gotchas_doc}` for a path this config sets; discern fills in its value before use and leaves other `${…}` text as written.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "kinds": {
            "description": "Only count files changed in these ways: \"added\", \"modified\", or \"deleted\". A rename counts as a deletion and an addition.",
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "added",
                "modified",
                "deleted"
              ]
            }
          },
          "adds_matching": {
            "description": "Only count text files where an added line contains one of these exact strings, matched case-sensitively. Up to 16 different strings of 1–128 UTF-8 bytes each, without NUL, CR, or LF. discern skips the check when a change is too large to scan, and records that it did.",
            "minItems": 1,
            "maxItems": 16,
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "removes_matching": {
            "description": "Only count text files where a removed line contains one of these exact strings, matched case-sensitively. Up to 16 different strings of 1–128 UTF-8 bytes each, without NUL, CR, or LF. discern skips the check when a change is too large to scan, and records that it did.",
            "minItems": 1,
            "maxItems": 16,
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "new_directory": {
            "description": "Only count files added in a folder that had no files where the branch started. Files added at the project root never count.",
            "type": "boolean"
          },
          "binary": {
            "description": "Only count binary files when true, or only text files when false.",
            "type": "boolean"
          },
          "min_changed_files": {
            "description": "Trigger only when at least this many watched files changed. Leave it out to trigger on any change.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "min_changed_lines": {
            "description": "Trigger only when the counted files have at least this many added and removed lines in total. Binary files count as zero.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "deletion_dominant": {
            "description": "Trigger only when the change mostly removes lines: removals clearly outnumber additions and pass a fixed minimum. A large cut gets reviewed; an ordinary edit or a balanced refactor doesn't.",
            "type": "boolean"
          },
          "similar_new_file": {
            "description": "Trigger only when the change adds a file named like an existing file in the same folder, such as a copy, new, or v2 version of it, which suggests a second version growing beside the first.",
            "type": "boolean"
          },
          "min_commits": {
            "description": "Trigger only when the branch has at least this many commits since it left the trunk, merge commits included. Uncommitted work doesn't count.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "when": {
            "description": "A command that makes the final decision, run only when every other setting matches. Exit 0 triggers the checkpoint, and exit 10 doesn't; `DISCERN_MATCH <path>` lines it prints narrow the matched files. Any other result, including running out of time, counts as undecided: a `stop` checkpoint then fires over every matched file, and landing needs the owner's approval in the conversation.",
            "type": "string"
          },
          "mode": {
            "description": "\"stop\" holds `discern done` until the agent answers the question as met or unmet; an unmet answer then needs the owner's variance to land. \"advise\" shows the question as advice and blocks nothing. Your own checkpoints default to \"stop\"; a built-in one keeps its own mode.",
            "type": "string",
            "enum": [
              "stop",
              "advise"
            ]
          },
          "question": {
            "description": "The question the agent answers, written here. Your own checkpoint needs this or `question_file`; a built-in one keeps its shipped question unless you set one. Review screens show it, so don't include secrets.",
            "type": "string"
          },
          "question_file": {
            "description": "A text file holding the question, relative to the repository root, used in place of `question`. discern reads it as committed where the branch started. It must be a regular file that Git tracks, valid UTF-8, and at most 65536 bytes. Its text can appear in the terminal, MCP, CI, Proof, and landing review, and only repository access keeps it private, so don't include secrets.",
            "type": "string",
            "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
          },
          "teach": {
            "description": "Optional: why the question matters and what a good answer looks like. discern shows it with the question.",
            "type": "string"
          },
          "reference": {
            "description": "Optional: a pointer to more detail, such as a doc path or web address. discern shows it as written and never opens or runs it. Don't include secrets.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    "setup": {
      "description": "`[setup]` settings for how setup counts known jobs.",
      "default": {},
      "type": "object",
      "properties": {
        "not_applicable": {
          "default": [],
          "description": "Known jobs this project doesn't have. A job listed here can't also appear under `[jobs]`, even with an empty command.",
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "format",
              "build",
              "lint",
              "typecheck",
              "test",
              "smoke"
            ]
          },
          "uniqueItems": true
        }
      },
      "additionalProperties": false
    },
    "worktree": {
      "description": "`[worktree]` settings for task worktrees and their resources.",
      "default": {},
      "type": "object",
      "properties": {
        "root": {
          "default": "",
          "description": "The folder where discern creates worktrees. Empty means a folder beside the repository, named \"<repo>.worktrees\". A relative path is relative to the repository root, and an absolute path is used as written. A change applies to new worktrees only.",
          "type": "string"
        },
        "inherit_env": {
          "default": [],
          "description": "Environment variables to copy from the main checkout's env files into each worktree's when it's set up. A worktree keeps its own value, unless that value is empty or still the placeholder from the first env file's `.example` copy. discern creates the first env file if it's missing, readable only by you (mode 0600), and leaves existing files' permissions alone.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "env_files": {
          "default": [
            ".env",
            ".env.local"
          ],
          "description": "The env files discern reads and writes, in order. When several set the same variable, the last one wins. discern updates a value where it already is, or adds it to the first listed file that exists; only `inherit_env` creates a file. One comment line at the top of each file marks the values discern manages.",
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^(?!.*\\s$)(?:\\.\\/)*(?![~\\s])(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ](?:\\/(?!(?:\\.{1,2}|\\.[gG][iI][tT])(?:\\/|$))(?!(?:(?:[cC][oO][nN]|[pP][rR][nN]|[aA][uU][xX]|[nN][uU][lL]|[cC][oO][mM][1-9]|[lL][pP][tT][1-9]))(?:\\.|\\/|$))[^<>:\"\\\\|?*\\u0000-\\u001F/]*[^<>:\"\\\\|?*\\u0000-\\u001F/. ])*$"
          },
          "uniqueItems": true
        },
        "export_port": {
          "default": false,
          "description": "Set to true to write each worktree's port into an env file that already exists, as `DISCERN_WORKTREE_PORT`. discern then avoids giving a new worktree a port another checkout uses, warns about a clash, and shows the port in `discern status`. Every worktree has a port either way; `discern identity --port` prints it.",
          "type": "boolean"
        },
        "track_ignored_drift": {
          "default": true,
          "description": "Record the Git-ignored top-level paths, such as `node_modules` or `.env`, when a worktree is set up, and list the ones that changed when its work lands. The list is for information only. Turn it off when ignored files change too often for it to help.",
          "type": "boolean"
        },
        "resources": {
          "default": {},
          "description": "Outside resources that each worktree gets its own copy of.",
          "type": "object",
          "propertyNames": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]+$"
          },
          "additionalProperties": {
            "type": "object",
            "properties": {
              "create": {
                "default": "",
                "description": "The command that creates the resource, run once when the worktree is first set up. Before running it, discern records the matching `destroy`, so if `create` fails partway, the next setup cleans up and tries again. Once `create` succeeds, it never runs again. An empty command does nothing.",
                "type": "string"
              },
              "destroy": {
                "default": "",
                "description": "The command that removes the resource when its worktree is removed, or before discern retries a failed `create`. Make it safe to run twice, and don't rely on the current folder: `discern worktree prune` may run it after the worktree is gone. discern keeps the command as it was when the resource was created.",
                "type": "string"
              },
              "ensure": {
                "default": "",
                "description": "A command that checks the resource and repairs it if needed, such as restarting a stopped database. It runs at each session start and when `discern worktree setup` runs again, but not at creation or after `discern update`. It must be safe to repeat, and a failure only warns.",
                "type": "string"
              },
              "required": {
                "default": true,
                "description": "Set to false to let setup finish when `create` fails. discern then doesn't retry `create`; only `ensure` runs later.",
                "type": "boolean"
              },
              "retries": {
                "default": 0,
                "description": "How many times to retry a failed `create`, `destroy`, or `ensure`, from 0 to 5. `destroy` uses the count in effect when the resource was created.",
                "type": "integer",
                "minimum": 0,
                "maximum": 5
              },
              "prunable": {
                "default": true,
                "description": "Set to false to stop `discern worktree prune` from destroying this resource after its worktree was deleted without discern, for data you can't afford to lose. Removing the worktree through discern still destroys it. The value in effect when the resource was created applies.",
                "type": "boolean"
              }
            },
            "additionalProperties": false
          }
        },
        "setup": {
          "description": "Commands that prepare a task worktree for work.",
          "default": {},
          "type": "object",
          "properties": {
            "steps": {
              "default": [],
              "description": "Commands run once, in order, when discern creates the worktree, after its resources exist. Use them for one-time setup, such as loading test data. A failing step stops creation, and steps you add later don't run in existing worktrees.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "ensure": {
              "default": [],
              "description": "Commands run whenever discern readies a task worktree: at creation, at each session start, when `discern worktree setup` runs again, and after `discern update`. Use them for setup that depends on the worktree's id, port, or resources. They must be safe to repeat, and only a failure at creation stops anything. The main checkout never runs them.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false,
  "description": "A setup recipe, format version 1, used only by `discern setup begin --config <file>`. The project's identity and setup answers sit at the top level; its map, jobs, scopes, generated groups, standards, checkpoints, setup, and worktree settings go into discern.toml. Standing landing grants can't be set here. Every field is optional."
}
