name: deploy site

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  deployments: read

jobs:
  publish:
    # Dispatch main after its complete gate succeeds. No tag or binary release.
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/site-publish.yml
